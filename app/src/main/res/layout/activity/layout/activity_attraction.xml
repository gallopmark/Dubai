<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:background="@color/white"
    android:layout_height="match_parent">

    <android.support.design.widget.TabLayout
        android:id="@+id/mTabLayout"
        app:tabIndicatorColor = "@color/red"
        app:tabSelectedTextColor = "@color/black"
        android:textColor = "@color/layoutLoading"
        app:tabMode="scrollable"
        android:visibility="gone"
        android:background="@color/white"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <com.uroad.dubai.widget.EnhanceTabLayout
        android:id="@+id/enhance_tab_layout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:tabIndicatorHeight="2dp"
        app:tabIndicatorWidth="30dp"
        app:tabTextColor="#999999"
        app:tab_Mode="mode_scrollable"
        app:tabSelectTextColor="@color/black"
        app:tabIndicatorColor="@color/red"
        app:tabTextSize="6sp" />

    <ImageView
        android:id="@+id/ivSearchAttr"
        android:layout_marginRight="@dimen/space_10"
        android:padding="@dimen/space_10"
        android:src="@mipmap/ic_search_grey"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@+id/enhance_tab_layout"
        android:layout_alignBottom="@+id/enhance_tab_layout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <android.support.v4.view.ViewPager
        android:id="@+id/mViewPager"
        android:layout_below="@+id/mTabLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>


</RelativeLayout>